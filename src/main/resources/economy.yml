# Экономика (требуется Vault + плагин экономики). Без них модуль неактивен.
enabled: true

# Стоимость создания пати (0 — бесплатно)
party-cost: 0.0

# Награда победителю боя (0 — без награды)
win-reward: 0.0

# Выдавать награду только за ранкед-бои (из очереди)
win-reward-ranked-only: true
